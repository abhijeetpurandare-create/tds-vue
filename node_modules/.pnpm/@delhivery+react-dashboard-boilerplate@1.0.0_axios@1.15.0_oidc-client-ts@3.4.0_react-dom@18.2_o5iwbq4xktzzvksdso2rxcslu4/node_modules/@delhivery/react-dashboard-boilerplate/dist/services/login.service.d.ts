import { UserManager } from 'oidc-client-ts';

declare class LoginService {
    private userManager;
    loadDiscoveryDocument: () => Promise<void>;
    configureSSO: (issuer: string, clientId: string, redirectUri: string) => Promise<void>;
    login: (issuer: string, clientId: string, redirectUri: string) => Promise<void>;
    clearStorage: () => void;
    logout: () => Promise<void>;
    getUserManager: () => UserManager | null;
    setUserManager: (manager: UserManager) => void;
    getAccessToken: () => Promise<string | null>;
    hasValidAccessToken: () => boolean;
}
export declare const loginService: LoginService;
export {};
//# sourceMappingURL=login.service.d.ts.map