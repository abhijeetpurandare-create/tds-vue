import { UserManagerSettings } from 'oidc-client-ts';

export interface ConfigOptions {
    configOptions: (issuer: string, clientId: string, redirectUri: string) => UserManagerSettings;
}
declare class OidcConfigService implements ConfigOptions {
    configOptions(issuer: string, clientId: string, redirectUri: string): UserManagerSettings;
}
export declare const oidcConfigService: OidcConfigService;
export {};
//# sourceMappingURL=oidc.config.d.ts.map