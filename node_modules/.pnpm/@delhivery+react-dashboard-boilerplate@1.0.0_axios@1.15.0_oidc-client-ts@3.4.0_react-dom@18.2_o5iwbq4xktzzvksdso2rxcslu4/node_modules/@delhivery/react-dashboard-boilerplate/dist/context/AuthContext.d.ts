import { ReactNode } from 'react';
import { UserManager, User } from 'oidc-client-ts';
import { AuthConfig } from '../types';

interface AuthContextType {
    user: User | null;
    isAuthenticated: boolean;
    isLoading: boolean;
    login: (issuer: string, clientId: string, redirectUri: string) => Promise<void>;
    logout: () => Promise<void>;
    getUserManager: () => UserManager | null;
}
interface AuthProviderProps {
    config: AuthConfig;
    children: ReactNode;
}
export declare const AuthProvider: ({ config, children }: AuthProviderProps) => import("react/jsx-runtime").JSX.Element;
export declare const useAuth: () => AuthContextType;
export {};
//# sourceMappingURL=AuthContext.d.ts.map