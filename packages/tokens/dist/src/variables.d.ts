/**
 * Tarmac Design System — CSS Variables (Design Tokens)
 *
 * Framework-agnostic design tokens extracted from the Tarmac TDS.
 * These are the canonical source of truth for colors, shadows, and
 * other visual primitives used across all framework implementations.
 *
 * Consumed by:
 *  - @delhivery/tarmac (React)
 *  - @tarmac/web-components (Web Components / Lit)
 *  - @tarmac/vue (Vue 3 wrappers)
 */
export declare const themeVariables: Record<string, string>;
export default themeVariables;
